import Layout from "../components/layout"
import { Typography, TextField, Button, Card, CardContent } from "@mui/material"
import Image from "next/image"

export default function Reviews() {
  return (
    <Layout>
      <div className="container mx-auto px-4 py-8">
        <h1 className="text-3xl font-bold mb-8">User Experiences & Reviews</h1>
        <div className="mb-12">
          <Typography variant="h5" className="mb-4">
            Add Your Experience
          </Typography>
          <Card>
            <CardContent>
              <TextField label="Beach Name" variant="outlined" fullWidth className="mb-4" />
              <TextField label="Your Review" variant="outlined" fullWidth multiline rows={4} className="mb-4" />
              <div className="mb-4">
                <input type="file" accept="image/*" className="mb-2" />
                <Typography variant="caption">Upload your beach photos</Typography>
              </div>
              <Button variant="contained" color="primary">
                Submit Review
              </Button>
            </CardContent>
          </Card>
        </div>
        <div className="mb-12">
          <Typography variant="h5" className="mb-4">
            User Photo Gallery
          </Typography>
          <div className="grid grid-cols-2 md:grid-cols-4 gap-4">
            {[1, 2, 3, 4, 5, 6, 7, 8].map((i) => (
              <Image
                key={i}
                src={`/placeholder.svg?height=150&width=150`}
                alt={`User Photo ${i}`}
                width={150}
                height={150}
                className="w-full rounded-lg"
              />
            ))}
          </div>
        </div>
        <div>
          <Typography variant="h5" className="mb-4">
            Top-Rated Reviews
          </Typography>
          <div className="grid grid-cols-1 md:grid-cols-2 gap-8">
            {[1, 2, 3, 4].map((i) => (
              <Card key={i}>
                <CardContent>
                  <Typography variant="h6" className="mb-2">
                    Amazing Beach Experience
                  </Typography>
                  <Typography variant="body2" color="text.secondary" className="mb-2">
                    Beach Name • Rating: 5/5
                  </Typography>
                  <Typography variant="body2" className="mb-4">
                    Lorem ipsum dolor sit amet, consectetur adipiscing elit. Aenean euismod bibendum laoreet.
                  </Typography>
                  <Typography variant="caption">By User{i} • Date: MM/DD/YYYY</Typography>
                </CardContent>
              </Card>
            ))}
          </div>
        </div>
      </div>
    </Layout>
  )
}

